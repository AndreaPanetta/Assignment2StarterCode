// If Player hits either the edges,boxes or other player, collision is detected and the player dies   
/*void Collision()
{
  for(int i = 0; i < sliders.length; i++)
  {
    if((sliders[i].x < 50 + 40))
    {
      if(sliders[i].y < (pos.y + 40) && sliders[i].y > pos.y || sliders[i].y + 20 < (pos.y + 40) && sliders[i].y + 20 > pos.y )
      {
        println(i);
        sliders[i].x = width;
        fill(0);
        stroke(0); //Player disappears if in contact with enemy
        }
      }
    }
  }*/




